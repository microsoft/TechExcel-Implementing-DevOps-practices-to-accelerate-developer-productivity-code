
name: first-workflow

on:
  workflow_dispatch:

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Step 1 complete!"
      - run: |
          echo "Step 2 complete!"
